# TJC4827T143_011C-P20 宋体字库 ID 对照

基准文件：`260728song.tft`（2026-07-28，SHA-256
`bf1bf6e637f4d10ab263286e1b52ac162a86cb7d5a34f513c6d04c8700d1c766`）。

目标屏幕：`TJC4827T143_011C-P20`

## 实际 TJC 字体 ID

`.tft` 中字体资源的出现顺序才是 `xstr` 使用的字体 ID；资源名称中的
`-0` 到 `-4` 后缀不是 ID。因此本宋体字库的 ID 与名称/字号是倒序关系。

| TJC `xstr` ID | `.tft` 资源名 | 实测字高 | 固件用途 |
|---:|---|---:|---|
| `0` | `font-4song` | 32 px | 学习结果的大号线序/点号 |
| `1` | `font-3song` | 29 px | PRINT 等结果字 |
| `2` | `font-2song` | 16 px | 页标题、K1–K4 主标题 |
| `3` | `font-1song` | 14 px | 状态栏、普通提示 |
| `4` | `font-0song` | 10 px | 自检/学习表格、辅助说明 |

## 空间核对

| 使用区域 | 可用文字高度 | 选用字高 | 结论 |
|---|---:|---:|---|
| 自检格 IN/OUT | 18 px | 10 px（ID 4） | 适合，余 8 px |
| 学习格 IN/OUT | 16 px | 10 px（ID 4） | 适合，余 6 px |
| 自动测试列表 | 14 px | 10 px（ID 4） | 适合，余 4 px |
| 状态栏/统计栏 | 23–33 px | 14 px（ID 3） | 适合 |
| 页标题、K1–K4 | 26–31 px | 16 px（ID 2） | 适合 |
| 自动测试 PASS、NG | 130 px | 32 px（ID 0，当前上限） | 适合；结果底色延伸至 K1–K4 上缘 |
| PRINT | 60–82 px | 29 px（ID 1） | 适合 |
| 学习大号线序 | 82 px | 32 px（ID 0） | 适合 |

不需要再缩小此宋体字库：最紧凑的学习格仍保留 6 px 高度余量。若以后重建
字库，请保持上述资源的**出现顺序**不变；仅保持文件名不变不足以保证 ID 不变。

自动测试总结页采用左侧 `RESULT` 2/5、右侧 `DETAILS` 3/5：PASS 的右侧依次
显示 `TOTAL`、`PAIRS`、`POINTS`；NG 的短线序保持一行，较长线序按完整的
`Ixxx`/`Oxxx` 端点换行，并优先让 I 组在上、O 组在下。
